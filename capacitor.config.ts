import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tcmcloud.health",
  appName: "中医云健康",
  webDir: "dist",
  android: {
    backgroundColor: "#f4f7fb"
  }
};

export default config;
